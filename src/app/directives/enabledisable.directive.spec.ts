import { EnabledisableDirective } from './enabledisable.directive';

describe('EnabledisableDirective', () => {
  it('should create an instance', () => {
    const directive = new EnabledisableDirective();
    expect(directive).toBeTruthy();
  });
});
